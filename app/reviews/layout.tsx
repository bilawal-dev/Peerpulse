export default function ReviewsDashboardPage({ children }: { children: React.ReactNode }) {

    return (
        <main className="p-8 max-sm:px-4 space-y-8 bg-gray-50">
            {children}
        </main>
    );
}
